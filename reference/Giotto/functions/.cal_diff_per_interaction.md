# `.cal_diff_per_interaction` {#.cal_diff_per_interaction}

*Package:* `Giotto`  
*Title:* Calculate difference per interaction

## Description

calculate correlation between expression residual and
cell proximity score of selected cell for spots

## Usage

```r
.cal_diff_per_interaction(
  sel_int,
  other_ints,
  select_ind,
  other_ind,
  proximityMat,
  expr_residual
)
```

## Value

data.table

